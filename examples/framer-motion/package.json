{
  "name": "test",
  "description": "",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "paperclip build --definition --write & webpack",
    "start": "paperclip build --definition --write --watch & webpack-dev-server",
    "build:watch": "paperclip build --definition --write --watch & webpack --watch",
    "build:definitions": "paperclip build --definition --write"
  },
  "license": "",
  "dependencies": {},
  "devDependencies": {}
}
