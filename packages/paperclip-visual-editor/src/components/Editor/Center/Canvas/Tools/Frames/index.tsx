import React from "react";
import * as styles from "./index.pc";

export type FramesProps = {};

export const Frames = (props: FramesProps) => {
  return <styles.FramesContainer></styles.FramesContainer>;
};
