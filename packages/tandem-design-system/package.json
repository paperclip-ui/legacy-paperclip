{
  "name": "tandem-design-system",
  "version": "18.1.2",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "build": "yarn build:definition && yarn build:pc && tsc",
    "build:definition": "paperclip-minimal build --definition --write --only=d.ts --output=src --verbose",
    "build:pc": "paperclip-minimal build --definition --write --verbose",
    "build:watch": "paperclip-minimal build --definition --watch --write"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "paperclip": "^18.1.2",
    "paperclip-cli-minimal": "^18.1.2",
    "paperclip-compiler-react": "^18.1.2"
  }
}
