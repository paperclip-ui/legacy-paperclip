{
  "name": "tandem-design-system",
  "version": "18.2.15",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "build": "yarn build:definition && yarn build:pc && tsc",
    "build:definition": "paperclip-minimal build --only=d.ts --output=src --verbose",
    "build:pc": "paperclip-minimal build --verbose",
    "build:watch": "paperclip-minimal build --watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "paperclip": "^18.2.15",
    "paperclip-cli-minimal": "^18.2.15",
    "paperclip-compiler-react": "^18.2.15"
  }
}
