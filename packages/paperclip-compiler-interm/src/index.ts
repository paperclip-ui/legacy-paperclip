import { IntermediatModule } from "./state";

export type TranslateIntermediateOptions = {
  assetPath: string;
};

export const translateIntermediate = (
  source: string,
  options: TranslateIntermediateOptions
): IntermediatModule => {
  return null;
};
