{
  "name": "paperclip",
  "version": "14.0.9",
  "description": "- real-time editing. Biggest priority is to have a bundler that  - Interoperability. Paperclip should work with any web framework.  - Type safety.  - Visually editable.",
  "main": "index.js",
  "scripts": {
    "test": "npm run test:node && cd native && cargo test",
    "lint": "eslint src",
    "test:node": "nyc mocha ./lib/test/**/*-test.js && nyc merge .nyc_output ../../.nyc_output/paperclip.json",
    "test:watch": "mocha ./lib/test --watch & (cd native && cargo watch -x test)",
    "build": "npm run build:wasm:node && npm run build:node",
    "build:watch": "tsc --watch & (cd native && cargo watch -i .gitignore -i \"pkg/*\" -s 'wasm-pack build --release --target nodejs')",
    "build:node": "tsc",
    "build:native": "cd native && cargo build --release",
    "build:wasm:node": "cd native && wasm-pack build --release --target nodejs"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@types/mocha": "^5.2.7",
    "chai": "^4.2.0",
    "crc32": "^0.2.2",
    "mocha": "^7.0.1",
    "paperclip-utils": "^14.0.9",
    "typescript": "^3.8.2"
  },
  "gitHead": "bc53692b56f676db2ce24f79efde8f52401c1159",
  "devDependencies": {
    "@types/node": "^14.0.20",
    "@typescript-eslint/eslint-plugin": "^3.6.1",
    "@typescript-eslint/parser": "^3.6.1",
    "eslint": "^7.4.0",
    "nyc": "^15.1.0"
  }
}
